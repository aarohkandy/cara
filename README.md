# Cadybara

AI-assisted CAD interface built for a Vercel-hosted preview.

## Run locally

```bash
npm install
npm run dev
```

The app starts on `http://127.0.0.1:5173` by default.

## Build

```bash
npm run build
```

Vercel is configured with `vercel.json` for a Vite static deployment.
